spring:
  application:
    name: vinylhub
  datasource:
    url: jdbc:mysql://localhost:3306/vinylhub_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true